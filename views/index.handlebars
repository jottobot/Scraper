<div class="container">

  <div class="row" id="scrapedarticles">
    <div class="col-8" id="articles"></div>
    <div class="col-4" id="notes"></div>
  </div>

  {{!-- {{#each article}}
  <div class="card">
    <h3 id="artCon" class="card-header">{{this.title}}</h3>
    <div class="row">
      <div class="col-lg-8 card-block">
        <p class="card-text">{{this.summary}}</p>
      </div>
      <div class="col-lg-4 align-self-end text-right">
        {{#if saved}}
        <button class="btn btn-secondary m-2 disabled">Article Saved</button>
        {{else}}
        <button class="btn btn-outline-danger m-2 btn-save" data-id="{{this._id}}">Save Article</button>
        {{/if}}
      </div>
    </div>
  </div>
  {{/each}} --}}


</div>

</body>